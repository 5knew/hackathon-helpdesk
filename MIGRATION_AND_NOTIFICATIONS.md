# Миграция базы данных и уведомления

## ✅ Выполнено

### 1. Миграция базы данных
Запущена миграция, которая создала:
- ✅ Таблицу `feedback` - для CSAT оценок
- ✅ Таблицу `ticket_history` - для истории изменений тикетов
- ✅ Таблицу `templates` - для шаблонов ответов
- ✅ Колонки `sla_deadline` и `is_escalated` в таблице `tickets`

**Команда для запуска миграции:**
```bash
cd backend
python migrate_new_tables.py
```

### 2. Уведомления для пользователей
Добавлена логика уведомлений:

**Когда пользователь комментирует:**
- ✅ Все админы получают уведомление о новом комментарии (как было)

**Когда админ/оператор отвечает:**
- ✅ Владелец тикета (пользователь) получает уведомление типа `ADMIN_REPLY`
- ✅ Заголовок: "Администратор ответил на ваш вопрос #..."
- ✅ Сообщение содержит первые 100 символов ответа

**Типы уведомлений:**
- `COMMENT` - новый комментарий от пользователя (для админов)
- `ADMIN_REPLY` - ответ администратора (для пользователя)
- `TICKET_CREATED` - новый тикет (для админов)
- `TICKET_UPDATED` - тикет обновлен
- `TICKET_CLOSED` - тикет закрыт
- `ASSIGNED` - тикет назначен оператору

## 🎯 Теперь можно:

1. **Ставить CSAT оценки** - таблица `feedback` создана, API работает
2. **Получать уведомления** - когда админ отвечает на ваш тикет
3. **Просматривать историю** - все изменения тикетов записываются
4. **Использовать шаблоны** - создавать и использовать шаблоны ответов

## 📝 Примечания

- Уведомления для админов о новых тикетах **не изменены** - работают как раньше
- Уведомления для пользователей появляются только когда админ/оператор отвечает
- Все уведомления отображаются в панели уведомлений на фронтенде

