# Настройка PostgreSQL и системы уведомлений

## ✅ Что было сделано:

1. **Настроена PostgreSQL база данных**
   - База данных `helpdesk_db` создана
   - Все таблицы инициализированы
   - Создан администратор по умолчанию

2. **Система ролей**
   - Добавлена роль `admin` в дополнение к `client` и `employee`
   - Администратор может получать уведомления о новых комментариях

3. **Система уведомлений**
   - Создана модель `Notification` для хранения уведомлений
   - При добавлении комментария пользователем, все админы получают уведомление
   - API эндпоинты для получения и управления уведомлениями

## 🔑 Учетные данные администратора:

- **Email:** `admin@helpdesk.com`
- **Password:** `admin123`

## 📡 API эндпоинты для уведомлений:

- `GET /notifications?user_id={user_id}` - Получить все уведомления пользователя
- `GET /notifications?user_id={user_id}&unread_only=true` - Только непрочитанные
- `GET /notifications/unread/count?user_id={user_id}` - Количество непрочитанных
- `PUT /notifications/{notification_id}/read?user_id={user_id}` - Пометить как прочитанное
- `PUT /notifications/read-all?user_id={user_id}` - Пометить все как прочитанные

## 🚀 Как использовать:

1. **Войдите как администратор:**
   - Email: `admin@helpdesk.com`
   - Password: `admin123`

2. **Пользователь создает тикет и добавляет комментарий**

3. **Администратор получает уведомление:**
   - Уведомление появляется в системе
   - Администратор может ответить на комментарий

4. **Проверка уведомлений:**
   - Откройте: `http://localhost:8002/docs`
   - Используйте эндпоинт `/notifications?user_id={admin_user_id}`

## 🔄 Перезапуск базы данных:

Если нужно пересоздать базу данных:

```powershell
cd backend
$env:PGPASSWORD="admin"  # Ваш пароль PostgreSQL
python init_postgres.py
```

## 📝 Примечания:

- Бэкенд теперь использует PostgreSQL по умолчанию
- Для использования SQLite установите: `$env:USE_SQLITE="true"`
- Все уведомления сохраняются в таблице `notifications`
- Администраторы автоматически получают уведомления о новых комментариях


