# 🔐 Быстрый старт: Аутентификация

## ✅ Добавлены эндпоинты для регистрации и входа!

### 📋 Доступные эндпоинты:

1. **POST /auth/register** - Регистрация пользователя
2. **POST /auth/login** - Вход пользователя
3. **POST /auth/login/form** - Вход через OAuth2 форму (для Swagger)
4. **GET /auth/me** - Информация о текущем пользователе
5. **POST /auth/logout** - Выход пользователя

---

## 🚀 Примеры использования

### 1. Регистрация

```bash
curl -X POST "http://localhost:8002/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "name": "Иван Иванов",
    "role": "client"
  }'
```

**Ответ:**
```json
{
  "id": "uuid",
  "email": "user@example.com",
  "name": "Иван Иванов",
  "role": "client",
  "created_at": "2025-12-05T..."
}
```

---

### 2. Вход

```bash
curl -X POST "http://localhost:8002/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

**Ответ:**
```json
{
  "access_token": "token_string",
  "token_type": "bearer",
  "user_id": "uuid",
  "email": "user@example.com",
  "name": "Иван Иванов",
  "role": "client"
}
```

---

### 3. Получение информации о себе

```bash
curl -X GET "http://localhost:8002/auth/me" \
  -H "Authorization: Bearer {access_token}"
```

---

### 4. Выход

```bash
curl -X POST "http://localhost:8002/auth/logout" \
  -H "Authorization: Bearer {access_token}"
```

---

## 📚 Swagger UI

Все эндпоинты доступны в Swagger UI:
👉 http://localhost:8002/docs

В Swagger UI можно:
- Зарегистрироваться через `/auth/register`
- Войти через `/auth/login/form` (кнопка "Authorize" в правом верхнем углу)
- Тестировать защищенные эндпоинты с токеном

---

## 🔒 Безопасность

**Текущая реализация:**
- Пароли хешируются (SHA-256)
- Токены хранятся в памяти

**Для продакшена рекомендуется:**
- Использовать `bcrypt` для паролей
- JWT токены с истечением срока
- Хранить токены в Redis

---

## ✅ Что было сделано:

1. ✅ Добавлено поле `password_hash` в модель `User`
2. ✅ Создан роутер `/auth` с 5 эндпоинтами
3. ✅ Реализована регистрация и вход
4. ✅ Добавлена поддержка токенов
5. ✅ Миграция БД выполнена
6. ✅ Документация создана

---

**Готово к использованию!** 🎉

