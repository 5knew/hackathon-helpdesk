# 🚀 Руководство по миграции на новую архитектуру БД

## Обзор изменений

Проект был рефакторен для использования **PostgreSQL** вместо SQLite и новой архитектуры базы данных с 11 таблицами согласно спецификации.

## 📋 Что изменилось

### Структура проекта

```
backend/
├── models/              # SQLAlchemy модели (11 таблиц)
│   ├── ticket.py
│   ├── user.py
│   ├── department.py
│   ├── operator.py
│   ├── category.py
│   ├── ml_model.py
│   ├── ai_prediction.py
│   ├── ai_auto_response.py
│   ├── ticket_message.py
│   ├── daily_stat.py
│   └── training_sample.py
├── schemas/             # Pydantic схемы для валидации
├── routers/             # API роутеры
│   └── tickets.py
├── services/            # Бизнес-логика
│   ├── ai_classifier.py
│   ├── ai_router.py
│   ├── auto_resolver.py
│   └── stats_service.py
├── database.py          # Конфигурация БД
├── init_db.py           # Скрипт инициализации БД
├── main_new.py          # Новый main.py
└── requirements.txt     # Обновленные зависимости
```

### Новые таблицы

1. **tickets** - основная таблица тикетов
2. **users** - пользователи (клиенты и сотрудники)
3. **departments** - подразделения
4. **operators** - операторы 2-й линии
5. **categories** - категории тикетов
6. **ml_models** - модели ИИ
7. **ai_predictions** - предсказания ИИ
8. **ai_auto_responses** - автоматические ответы
9. **ticket_messages** - сообщения в тикетах
10. **daily_stats** - дневная статистика
11. **training_samples** - данные для обучения ML

## 🔧 Установка и настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE helpdesk_db;
```

Или используйте Docker:

```bash
docker run --name postgres-helpdesk \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=helpdesk_db \
  -p 5432:5432 \
  -d postgres:15
```

### 3. Настройка переменных окружения

Создайте файл `.env`:

```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/helpdesk_db
ML_SERVICE_URL=http://localhost:8000
```

### 4. Инициализация базы данных

```bash
python init_db.py
```

Это создаст все таблицы и заполнит начальными данными:
- Департаменты (General Support, Billing, Tech Support, HR, Customer Service)
- Категории (Общие вопросы, Биллинг, Техническая поддержка, и т.д.)
- Дефолтная ML модель

## 🔄 Миграция данных (опционально)

Если у вас есть данные в старой SQLite базе, можно создать скрипт миграции:

```python
# migrate_from_sqlite.py
import sqlite3
from sqlalchemy.orm import Session
from database import SessionLocal
from models import *

# Подключение к старой БД
sqlite_conn = sqlite3.connect('tickets.db')

# Подключение к новой БД
db = SessionLocal()

# Миграция данных...
# (детали зависят от структуры старой БД)
```

## 🚀 Запуск приложения

### Вариант 1: Использовать новый main_new.py

```bash
uvicorn main_new:app --host 0.0.0.0 --port 8002
```

### Вариант 2: Переименовать main_new.py в main.py

```bash
mv main_new.py main.py
python run.py
```

## 📝 Основные изменения в API

### Создание тикета

**Старый endpoint:**
```
POST /submit_ticket
```

**Новый endpoint:**
```
POST /tickets/create
```

**Новая структура запроса:**
```json
{
  "source": "email",  // email, chat, portal, phone
  "user_id": "uuid",
  "subject": "Тема",
  "body": "Текст обращения",
  "language": "ru"  // ru, kk, en
}
```

### Получение тикета

```
GET /tickets/{ticket_id}
```

### Список тикетов

```
GET /tickets?skip=0&limit=50&status=new
```

## 🔍 Проверка работы

1. Проверьте подключение к БД:
```python
from database import engine
print(engine.url)
```

2. Проверьте созданные таблицы:
```sql
\dt  -- в psql
```

3. Проверьте API:
```bash
curl http://localhost:8002/health
```

## ⚠️ Важные замечания

1. **UUID вместо INTEGER**: Все ID теперь UUID, а не целые числа
2. **ENUM типы**: Используются строгие enum типы для статусов, приоритетов и т.д.
3. **Relationships**: Модели связаны через SQLAlchemy relationships
4. **Миграции**: Для продакшена рекомендуется использовать Alembic для миграций

## 📚 Дополнительные ресурсы

- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)

## 🆘 Решение проблем

### Ошибка подключения к БД

Проверьте:
- Запущен ли PostgreSQL
- Правильность DATABASE_URL
- Существует ли база данных

### Ошибки импорта

Убедитесь, что все зависимости установлены:
```bash
pip install -r requirements.txt
```

### Проблемы с UUID

В PostgreSQL должен быть установлен расширение:
```sql
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
```

